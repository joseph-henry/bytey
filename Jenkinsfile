pipeline {
    agent any

    stages {
        stage('byte') {
            steps {
               sh 'echo byte byte byte'
               sh 'echo byte byte byte'            
            }
        }
        stage('byte') {
            steps {
                sh 'whoami'
                sh 'pwd'
            }
        }
    }
}
